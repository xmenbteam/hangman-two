const compArr = (usedLetters, wordArray) => {
  const sortedUsed = usedLetters.sort();
  return sortedUsed;
  // const pureWord = [];
};

compArr(usedLetters, wordArray);

module.exports = { compArr };
